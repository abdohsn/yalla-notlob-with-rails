<!DOCTYPE html>
<html>
  <head>
    <title>YallaNotlob</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="jquery/jquery.js"></script>


  </head>

  <body>
    <div class="container">
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">Yalla Notlob</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <%= link_to "Home" , home_index_path ,class: "nav-link active" %>
              </li>
              <li class="nav-item dropdown">
                <%= link_to "Friends", id: "navbar-dropdown", class: "nav-link dropdown-toggle", data: { toggle: "dropdown" }, aria: { haspopup: true, expanded: false } do %>
                Friends <% end %>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                 <a href="/friends/new" class= "dropdown-item">Add New Friend</a>
                 <li><hr class="dropdown-divider"></li>
                  <li><%= link_to "Show All Friends" , friends_path,class: "dropdown-item" %></li>
                </ul>
              </li>
              <li class="nav-item dropdown">
                <%= link_to "Groups", id: "navbar-dropdown", class: "nav-link dropdown-toggle", data: { toggle: "dropdown" }, aria: { haspopup: true, expanded: false } do %>
                Groups <% end %>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                 <a href="/groups/new" class= "dropdown-item">Add New Group</a>
                 <li><hr class="dropdown-divider"></li>
                 <a href="/groups/show" class= "dropdown-item">Show All Groups</a>
                </ul>
              </li>
              <%# <% if current_user %>
                <%# <%= image_tag(current_user.avatar_thumbnail) %> 
              <%# <% end %> 
              <li class="nav-item">
                <a href="/orders" class= "nav-link active">Orders</a>
              </li>
            </ul>
            <%
=begin%>
 <form class="d-flex">
              <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
              <button class="btn btn-outline-success" type="submit">Search</button>
            </form> 
<%
=end%>
            <% if user_signed_in? %>
            <%= link_to 'logout', destroy_user_session_path, method: :delete, class:"m-1 btn btn-outline-danger" %>
            <% end %>
          </div>
        </div>
      </nav>
      <% if user_signed_in? %>
        Welcome! <%= current_user.full_name %>
      <% end %>
      <% if notice.present? %>
      <p class="alert alert-primary"><%= notice %></p>
      <% end %>
      <% if alert.present? %>
      <p class="alert alert-danger"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
